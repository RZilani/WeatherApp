{
    "version": "0.2.0",
    "configurations": [
      {
        "name": "FastAPI : WeatherApp APIs",
        "type": "debugpy",
        "request": "launch",
        "module": "uvicorn",
        "args": [
          "main:app",
          "--reload",
          "--host", "0.0.0.0",
          "--port", "8000"
        ],
        "console": "integratedTerminal",
        "cwd": "${workspaceFolder}/backend/app",
        "python": "${workspaceFolder}/backend/.venv/bin/python"
      }
    ]
  }